# Secrets — NEVER commit (these hold wallet mnemonics / addresses)
.env
.env.*
!.env.template

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# Python
__pycache__/
*.pyc
.venv/
venv/

# Runtime artifacts
payments.jsonl
*.sqlite
*.sqlite3
*.db

# Local media
*.jpg
*.jpeg
*.JPG
*.JPEG

# Tooling / local
.claude/
.DS_Store

# Client mobile app (volt-connect) — its own git repo, synced from Lovable.
# Ignored here so the two gits never interfere. Pull UI updates with:
#   cd consumer/app && git pull
consumer/app/
